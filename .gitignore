node_modules/
dist/
coverage/
.DS_Store
*.log
logs/
tmp/
/.opencode-agent/

# Local runtime/dogfood state and other-tool configs — not part of the CLI repo.
/.apeironcode-agent/
/.agents/
/.codex/
/.claude/
*.tgz

# Local secrets — never commit. .env.example is the committed template.
.env
.env.local
.env.*.local

# Standalone marketing site (separate Next.js project, not part of the agent)
/ApeironCode-web/
# Scratch/output dirs from local tooling
/.playwright-mcp/
.next/

# Private IDE integration work is not part of the CLI-only public alpha.
/extensions/vscode/
