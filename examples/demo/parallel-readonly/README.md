# Parallel Read-Only Demo

```bash
../../../dist/cli/index.js team run "review auth and security" --parallel-readonly --dry-run
```

The command shows read-only lane scheduling without running editing lanes in parallel.
