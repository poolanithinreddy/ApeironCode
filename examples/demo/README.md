# ApeironCode Agent Demo

This demo uses the local repository and mock-safe commands. It does not require paid API keys.

```bash
npm run build
npm run demo:provider
npm run demo:skills
npm run demo:memory
npm run demo:context
npm run demo:github
```

For a narrated flow, see `docs/demo-script.md`.
