# Live Cockpit Demo Fixture

Use this fixture as a disposable target for demonstrating the mounted review cockpit.

Recommended flow:

```bash
npm run build
node dist/cli/index.js team cockpit <teamRunId>
```

For the interactive TUI, run `apeironcode`, then `/team cockpit <teamRunId>`.
