# Cockpit Memory Demo Fixture

Memory suggestions related to a team run appear in the cockpit memory pane. Use:

```bash
node dist/cli/index.js memory review --source team
```

Inside the TUI, `/team cockpit <teamRunId>` shows related pending suggestions when their related session id or facts mention the team run id.
